kind = "web"
previewPath = "/"
title = "Bahar - Premium Saudi Products"
version = "1.0.0"
id = "artifacts/bahar"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5000

[services.development]
run = "pnpm --filter @workspace/bahar run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/bahar", "run", "build" ]
publicDir = "artifacts/bahar/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5000"
BASE_PATH = "/"
